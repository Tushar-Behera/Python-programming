# import "plyer" to use for notification use --> pip install plyer
from plyer import notification
notification.notify( title="ORO - v 1.0 ",
                    message="Sir your deployment for this notification is stable..!!\n& working properly 🤓 Lets upgrade into v2.0",
                    app_icon= None,
                    app_name="OrO Notifier",
                    timeout=10,
                    ) # do not miss comma while separating the keys